# Feature specifications

New Spec Kit feature artifacts live here. See [the workflow guide](../spec-kit.md).
The root `specs` symlink points to this directory for upstream compatibility.
Existing sprint history stays in `docs/sprints/`; link it from the relevant feature.
